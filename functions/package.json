{
  "name": "noctua-forest-functions",
  "private": true,
  "engines": { "node": "18" },
  "type": "module",
  "scripts": {
    "build": "echo 'ESM functions, no build step'",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.1.0"
  }
}

